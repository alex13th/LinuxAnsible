node {
    stage("Getting repository") {
        checkout scm
    }
    stage("YAML-lint") {
        sh "yamllint -v ./ansible/base"
    }
    stage("Ansible-lint") {
        sh "ansible-lint -p -v ./ansible/base"
    }
}